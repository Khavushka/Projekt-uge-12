extends layout

block content
    h1= title
    p Show tasks here
    main
        div.login
                each task in tasks
                    div.login-container
                        div.login-header
                                div #{task.title}
                                div #{task.description}
                                div #{task.expires}
                                div #{task.priority}
                                div #{task.status}